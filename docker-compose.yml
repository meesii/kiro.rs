services:
  kiro-rs:
    image: kiro-rs:latest
    pull_policy: never
    container_name: kiro-rs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8990:8990"
    volumes:
      - ./:/app/config/
    restart: unless-stopped
